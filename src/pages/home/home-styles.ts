import { tv } from 'utils';

export const homeContainerStyles = tv({
  base: ['container mx-auto px-[24px] pb-[42px] pt-[42px]'],
});

export const homeTitleStyles = tv({
  base: ['bg-orange-100'],
});
